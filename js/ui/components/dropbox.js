;(function(ui){

    var _ons = {
        drop: function(e){

            return;
        }
    };

    var DropdownComponent = function (elem, options) {
        options || (options = {});

        this.el = options.el;
        _addEventListeners.call(this);
    }

    /** Events */

    function _addEventListeners(){

        return this;
    }

    /** Files in dropbox */

}(ui));